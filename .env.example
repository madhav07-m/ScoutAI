# Copy to .env and fill in for local dev if you don't want the hardcoded
# defaults in src/lib/supabase.js. Vite only exposes VITE_-prefixed vars
# to client code.
VITE_SUPABASE_URL=https://itlxwaictkckmuxpxvxs.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_4x4kQ2uzmSw_PMnRnYBdiA_CgmrFo74

# Only needed if the backend is deployed at a different origin than the
# frontend (e.g. frontend on Vercel, backend on Render). Leave unset for
# same-origin deploys where FastAPI serves the built frontend directly.
# VITE_API_BASE=https://scoutai-4yr0.onrender.com
